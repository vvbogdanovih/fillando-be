services:
    api:
        build:
            context: .
            dockerfile: Dockerfile
        container_name: fillando-be
        restart: unless-stopped
        env_file: .env.prod
        ports:
            - '4000:4000'
